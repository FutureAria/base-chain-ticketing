# 로드맵

지금 무엇이 되고, 무엇이 안 되며, 다음에 무엇을 할지 적어 둡니다.
각 항목은 이슈로 올라가 있거나 올라갈 예정입니다.

## 지금 되는 것 (v1.0.0)

- 경기 예매 → 좌석 선택 → 권종·포인트 적용 → 결제(mock) → 내 입장권
- 10초마다 회전하는 HMAC QR 검표, 재입장 차단
- 티켓 재판매 등록·구매 (MetaMask 서명으로 판매자 확인, 정가 초과 차단)
- 포인트·멤버십 티어(베이직/브론즈/실버/골드)와 티어 혜택
- 우선 예매 응모·추첨, NFT 조각 합성, 실물 굿즈 교환
- Oracle Cloud + Caddy HTTPS 배포

## 다음 (v1.1)

| 항목 | 왜 필요한가 | 크기 |
|---|---|---|
| **mock Fabric 상태 영속화** | 지금은 인메모리라 서버를 재시작하면 티켓 등록·예약·응모 기록이 사라집니다. 포인트·멤버십만 `point_events`에서 복구됩니다 | 중 |
| **비밀번호 재설정 메일 발송** | 토큰 발급까지는 구현돼 있고 메일 연동만 남았습니다. 지금은 서버 로그로 토큰을 확인해야 합니다 | 소 |
| **에러 알림 연동** | `/api/health`는 있지만 죽었을 때 아무도 모릅니다 | 소 |
| **프론트 번들 분할** | `index` 청크가 576KB입니다. 라우트 단위 동적 import가 필요합니다 | 소 |
| **API 문서 자동 생성** | 지금은 `docs/API.md`를 손으로 씁니다. 코드와 어긋날 수 있습니다 | 중 |

## 그다음 (v1.2)

| 항목 | 왜 필요한가 | 크기 |
|---|---|---|
| **테스트넷 온체인 발급 단계적 연결** | 컨트랙트는 작성·테스트돼 있으나 가스비 때문에 기본 비활성입니다. 발급 건수 상한을 두고 켜는 방식이 필요합니다 | 대 |
| **로그인 토큰을 httpOnly 쿠키로** | 지금은 `localStorage`입니다. CSRF 대응이 함께 필요해 미뤄 뒀습니다 | 중 |
| **좌석 배치도 실제 구장 좌표 반영** | 현재는 블록·열·번호 기반입니다 | 중 |
| **다국어(i18n)** | 현재 한국어 전용입니다 | 중 |

## 하지 않기로 한 것

범위를 지키기 위해 **의도적으로 뺀 것**들입니다. 필요해지면 다시 논의합니다.

| 항목 | 이유 |
|---|---|
| 자체 결제 모듈 | 결제는 PG사에 맡깁니다. 직접 만들면 보안 책임만 늘어납니다 |
| 실시간 좌석 소켓 동기화 | 좌석 확정은 DB 유니크 제약이 합니다. 소켓은 "보이는 것"만 바꿔서 오히려 상태가 어긋납니다 |
| 자체 지갑 구현 | MetaMask 등 기존 지갑을 씁니다. 키 보관 책임을 지지 않습니다 |
| 좌석 자동 추천 AI | 예매 흐름의 상태 정합성이 먼저입니다 |

## 참여하고 싶다면

- 위 항목 중 하나를 골라 이슈에 "제가 해보겠습니다"라고 댓글을 남겨 주세요
- 처음이시라면 [`good first issue`](https://github.com/FutureAria/base-chain-ticketing/issues?q=is%3Aissue+is%3Aopen+label%3A%22good+first+issue%22)부터
- 절차는 [`CONTRIBUTING.md`](CONTRIBUTING.md)에 있습니다
